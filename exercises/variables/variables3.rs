// variables3.rs
// Execute `rustlings hint variables3` or use the `hint` watch subcommand for a hint.

fn main() {
    // i'm wondering what the differences are between const & let.
    const x: i32 = 9;
    println!("Number {}", x + 3);
}
